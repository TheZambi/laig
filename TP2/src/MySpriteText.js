class MySpriteText {
    /**
     * @method constructor
     * @param scene Reference to MyScene object
     * @param text text to be displayed
     */
    constructor(scene, text) {
       this.scene = scene;
        this.text = text;
    }
}
